---
phase: 01-scrolling-track-restart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dino.js
  - dino.css
autonomous: true
requirements:
  - BG-01

must_haves:
  truths:
    - "While the game is running, the ground visibly scrolls left as a tiled sequence of track.png (no gap, no jitter)."
    - "After the dino dies, pressing any key clears the cacti, resets the score and dino state, and starts a new run without reloading the page."
    - "The game's overall feel — physics, collision, score increment — remains unchanged from before this phase."
  artifacts:
    - path: "dino.js"
      provides: "Track image globals, two-instance scrolling state, per-frame scroll/wrap/draw block"
      contains: "let trackImg"
    - path: "dino.js"
      provides: "Track entity initialization in window.onload"
      contains: "trackImg.src = \"./img/track.png\""
    - path: "dino.css"
      provides: "Sky color matches Chrome dino (white) instead of lightgray"
      contains: "background-color: white"
  key_links:
    - from: "update() in dino.js"
      to: "track1, track2 globals"
      via: "per-frame x += velocityX, wrap check, drawImage"
      pattern: "track1\\.x \\+= velocityX"
    - from: "update() track block"
      to: "velocityX (existing global)"
      via: "direct read — no parallel trackVelocityX"
      pattern: "track1\\.x \\+= velocityX"
    - from: "window.onload"
      to: "trackImg, track1, track2"
      via: "new Image() + src assign + object literal init"
      pattern: "trackImg = new Image\\(\\)"
---

<objective>
Render `img/track.png` (2404×28) as a tiled, left-scrolling ground strip on the canvas using the two-instance wrap pattern locked by D-01. Replace the flat `lightgray` CSS background with `white` so the sky above the track strip matches the real Chrome dino game (closes BG-01). Scroll speed reuses the existing `velocityX` global (D-02) so the track moves in lockstep with cacti.

Purpose: Closes requirement BG-01 (scrolling track replaces flat background) and delivers the first of three ROADMAP success criteria. This is the smallest visible "feels like Chrome dino" win in the v1.0 milestone.

Output: `dino.js` gains track state globals, track-init in `window.onload`, and a track scroll/wrap/draw block at the top of `update()`. `dino.css` gains `background-color: white` (replacing `lightgray`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scrolling-track-restart/01-CONTEXT.md
@.planning/phases/01-scrolling-track-restart/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@dino.js
@dino.css

<interfaces>
<!-- Existing dino.js entities use this shape; track entities follow the same shape so the codebase stays consistent. -->
<!-- Track entities are NEVER passed to detectCollision() — they are draw-only. -->

Existing entity shape (cacti and dino both satisfy this):
```javascript
{ x: number, y: number, width: number, height: number }
// Cacti additionally have: img: HTMLImageElement
```

Track entity shape (D-06 — match cactus shape, add img field):
```javascript
{ img: HTMLImageElement, x: number, y: number, width: number, height: number }
```

Existing globals this plan READS (do NOT redeclare or shadow):
- `boardWidth = 750`, `boardHeight = 250` (dino.js:4-5)
- `velocityX = -8` (dino.js:38) — track scroll speed reuses this directly
- `context` (dino.js:6, assigned in window.onload at dino.js:50) — used for drawImage

Existing call sites this plan MODIFIES:
- `window.onload` (dino.js:45-74) — append track image load + track1/track2 init after the cactus image block (dino.js:62-69)
- `update()` (dino.js:76-108) — insert track block immediately after `clearRect` (dino.js:81), before the `//dino` block at dino.js:83
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add track globals + load track image + initialize track1/track2 in window.onload</name>
  <files>dino.js</files>
  <read_first>
    - dino.js (full file, 167 lines — read it once before editing)
    - .planning/phases/01-scrolling-track-restart/01-CONTEXT.md sections D-01, D-02, D-03, D-05, D-06, D-16
    - .planning/phases/01-scrolling-track-restart/01-RESEARCH.md "Touchpoint 1" and "Touchpoint 2" (the literal code snippets to insert)
  </read_first>
  <behavior>
    - After this task: `dino.js` declares `trackImg`, `trackWidth = 2404`, `trackHeight = 28`, `trackX = 0`, `trackY = boardHeight - trackHeight`, `track1`, `track2` as module-level `let` globals.
    - After this task: `window.onload` constructs `trackImg`, sets `trackImg.src = "./img/track.png"`, and initializes `track1` and `track2` as object literals back-to-back (`track1.x = trackX = 0`, `track2.x = trackX + trackWidth = 2404`).
    - The file must still parse and load in a browser (open `index.html` — game still runs as before; track is not yet drawn because Task 2 inserts the draw block).
    - No `const` is introduced. No existing `let` declarations are modified.
  </behavior>
  <action>
**Edit 1 — Add track globals after the physics block.**

Locate the physics block in `dino.js` ending at line 43 (`let score = 0;`). Insert a NEW block after line 43 (before the blank line that precedes `window.onload` at line 45). Insert literally:

```javascript

//track
let trackImg;
let trackWidth = 2404;  //track.png native width
let trackHeight = 28;   //track.png native height
let trackX = 0;
let trackY = boardHeight - trackHeight; //222 — bottom-aligned under dino/cactus feet
let track1;
let track2;
```

Style notes (per D-16, CLAUDE.md, and the existing `let cactusHeight = 70` precedent):
- Use `let` for every declaration. Do NOT use `const`.
- Keep the inline `//comment` after numeric magic numbers — match the existing convention (`let velocityX = -8; //cactus moving left speed`).
- Variable names match the local convention: lowercase, no underscores, descriptive (`trackImg`, not `track_img` or `trackImage`).

**Edit 2 — Load track image and initialize track1/track2 inside `window.onload`.**

Locate the cactus image block in `window.onload` at `dino.js:62-69`:

```javascript
    cactus1Img = new Image();
    cactus1Img.src = "./img/cactus1.png";

    cactus2Img = new Image();
    cactus2Img.src = "./img/cactus2.png";

    cactus3Img = new Image();
    cactus3Img.src = "./img/cactus3.png";
```

Insert AFTER the cactus3Img block (still inside `window.onload`, BEFORE `requestAnimationFrame(update);` at line 71). Insert literally:

```javascript

    trackImg = new Image();
    trackImg.src = "./img/track.png";

    track1 = { img: trackImg, x: trackX,              y: trackY, width: trackWidth, height: trackHeight };
    track2 = { img: trackImg, x: trackX + trackWidth, y: trackY, width: trackWidth, height: trackHeight };
```

Notes (per D-05, D-06):
- No `onload` guard on `trackImg` — match the cactus fire-and-forget pattern. The dino-image `onload` at `dino.js:58-60` is a one-time first-frame guard, NOT a per-image policy.
- `track1.x = 0` (visible from the left edge); `track2.x = 2404` (immediately to the right of `track1`, off-screen-right at frame 0, ready to scroll in).
- Indentation: 4 spaces, matching the existing `window.onload` body.

**DO NOT:**
- Do NOT introduce `const` anywhere.
- Do NOT modify `dinoY`, `velocityX`, `velocityY`, `gravity`, `dinoWidth`, `dinoHeight`, or any existing global.
- Do NOT move existing code.
- Do NOT push `track1`/`track2` into `cactusArray` (D-06 — they are NOT cacti).
  </action>
  <verify>
    <automated>cd /Users/benglasser/git/chrome-dinosaur-game &amp;&amp; node --check dino.js &amp;&amp; grep -q '^let trackImg;' dino.js &amp;&amp; grep -q '^let trackWidth = 2404;' dino.js &amp;&amp; grep -q '^let trackHeight = 28;' dino.js &amp;&amp; grep -q '^let trackY = boardHeight - trackHeight;' dino.js &amp;&amp; grep -q '^let track1;' dino.js &amp;&amp; grep -q '^let track2;' dino.js &amp;&amp; grep -q 'trackImg = new Image();' dino.js &amp;&amp; grep -q 'trackImg.src = "./img/track.png";' dino.js &amp;&amp; grep -q 'track1 = { img: trackImg' dino.js &amp;&amp; grep -q 'track2 = { img: trackImg' dino.js &amp;&amp; ! grep -E '^\s*const\s+(trackImg|trackWidth|trackHeight|trackX|trackY|track1|track2)' dino.js &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check dino.js` exits 0 (file parses).
    - `dino.js` contains `let trackImg;` AND `let trackWidth = 2404;` AND `let trackHeight = 28;` AND `let trackY = boardHeight - trackHeight;` AND `let track1;` AND `let track2;`.
    - `dino.js` contains `trackImg = new Image();` AND `trackImg.src = "./img/track.png";`.
    - `dino.js` contains a `track1 = { img: trackImg` initializer AND a `track2 = { img: trackImg` initializer.
    - `dino.js` does NOT contain `const trackImg`, `const trackWidth`, `const trackHeight`, `const trackX`, `const trackY`, `const track1`, or `const track2` anywhere on a line introduced by this task (preserves tutorial style — D-16).
    - The order of declarations in window.onload places trackImg loading AFTER `cactus3Img.src = "./img/cactus3.png";` AND BEFORE `requestAnimationFrame(update);`.
    - `grep -c 'requestAnimationFrame' dino.js` is still 2 (one in `window.onload`, one as the first line of `update()` — existing pattern preserved).
  </acceptance_criteria>
  <done>
    - All grep-verifiable acceptance criteria pass.
    - File still parses with `node --check dino.js`.
    - Browser load of `index.html` still runs the game with no console errors (track not yet drawn — Task 2 wires the draw call).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert track scroll-wrap-draw block at top of update() and switch CSS sky to white</name>
  <files>dino.js, dino.css</files>
  <read_first>
    - dino.js (re-read after Task 1 — lines 76-108 for `update()` and confirm Task 1 globals are present)
    - dino.css (5 lines)
    - .planning/phases/01-scrolling-track-restart/01-CONTEXT.md sections D-01, D-02, D-04, D-08
    - .planning/phases/01-scrolling-track-restart/01-RESEARCH.md "Touchpoint 3" (literal code), "CSS change — dino.css line 7", "Wrap Math Explained", "Common Pitfalls" (esp. Pitfall 3 on `<= 0` wrap condition)
  </read_first>
  <behavior>
    - After this task: every frame, `update()` advances `track1.x` and `track2.x` by `velocityX` (-8 px/frame), wraps each when its right edge exits the canvas (`track.x + track.width <= 0`), and draws both via `context.drawImage(...)`.
    - The track block executes AFTER `clearRect` AND AFTER the `gameOver` early-return (so track halts on game-over per D-08), and BEFORE the existing `//dino` and `//cactus` blocks (D-04 — track paints first, dino/cacti paint over it).
    - On page load, the track is visible from frame 1, scrolls at the same speed as cacti (both use `velocityX`), and shows no gap or jitter at the wrap seam after ~5 seconds of play.
    - `dino.css` `#board` background-color is `white` (was `lightgray`). Sky above the track strip reads white in the browser.
  </behavior>
  <action>
**Edit 1 — Insert track scroll-wrap-draw block in `update()`.**

Locate `update()` body in `dino.js`. Confirm the existing structure:

```javascript
function update() {
    requestAnimationFrame(update);          // line 77
    if (gameOver) {                          // line 78
        return;                              // line 79
    }                                        // line 80
    context.clearRect(0, 0, board.width, board.height);   // line 81
                                             // line 82 (blank)
    //dino                                   // line 83
    velocityY += gravity;
    ...
```

Insert AFTER `context.clearRect(...)` at line 81 (i.e., after line 81, BEFORE the blank line 82 that precedes `//dino`). Insert literally:

```javascript

    //track
    track1.x += velocityX;
    track2.x += velocityX;
    if (track1.x + track1.width <= 0) {
        track1.x = track2.x + track2.width;
    }
    if (track2.x + track2.width <= 0) {
        track2.x = track1.x + track1.width;
    }
    context.drawImage(track1.img, track1.x, track1.y, track1.width, track1.height);
    context.drawImage(track2.img, track2.x, track2.y, track2.width, track2.height);
```

Style/correctness notes:
- Use `<= 0` (NOT `< 0`). Pitfall 3 in RESEARCH.md: a strict `<` leaves a 1-pixel gap that flickers at 60 fps.
- Wrap target is `other.x + other.width` (no offset). Keeps the seam at exactly zero — no gap, no overlap.
- Use the existing `velocityX` global directly (D-02). DO NOT introduce a `trackVelocityX`.
- Indentation: 4 spaces, matching the existing `update()` body.
- The block sits AFTER the `if (gameOver) { return; }` early-return (D-08 — track halts on game-over).
- The block sits BEFORE the `//dino` and `//cactus` blocks (D-04 — track is bottom of the entity stack; dino and cacti paint over it).

**Edit 2 — Replace `lightgray` with `white` in `dino.css`.**

Locate `dino.css:7`:

```css
    background-color: lightgray;
```

Replace with:

```css
    background-color: white;
```

Notes (per RESEARCH.md "CSS change" and Risk 1):
- The track strip occupies y=222–250. `clearRect` clears the upper 222px to transparent each frame, so the canvas's CSS background-color shows through above the track. White matches the real Chrome dino sky.
- DO NOT remove the `border-bottom: 1px solid black;` line — it stays.
- DO NOT add any other CSS rules in this task.

**DO NOT:**
- Do NOT cancel or restart `requestAnimationFrame` or `setInterval`.
- Do NOT modify `velocityX`, `velocityY`, `gravity`, `dinoY`, jump velocity (`-10`), or any physics constant.
- Do NOT touch the dino, cactus, score, or collision code.
- Do NOT introduce `const`.
- Do NOT call `detectCollision` on `track1`/`track2` (D-07 — track does not collide).
  </action>
  <verify>
    <automated>cd /Users/benglasser/git/chrome-dinosaur-game &amp;&amp; node --check dino.js &amp;&amp; grep -q 'track1.x += velocityX;' dino.js &amp;&amp; grep -q 'track2.x += velocityX;' dino.js &amp;&amp; grep -q 'if (track1.x + track1.width &lt;= 0)' dino.js &amp;&amp; grep -q 'if (track2.x + track2.width &lt;= 0)' dino.js &amp;&amp; grep -q 'context.drawImage(track1.img' dino.js &amp;&amp; grep -q 'context.drawImage(track2.img' dino.js &amp;&amp; grep -q 'background-color: white;' dino.css &amp;&amp; ! grep -q 'background-color: lightgray' dino.css &amp;&amp; ! grep -q 'trackVelocityX' dino.js &amp;&amp; [ "$(grep -c 'requestAnimationFrame' dino.js)" = "2" ] &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check dino.js` exits 0 (file parses).
    - `dino.js` contains `track1.x += velocityX;` AND `track2.x += velocityX;`.
    - `dino.js` contains `if (track1.x + track1.width <= 0)` AND `if (track2.x + track2.width <= 0)` (with `<=`, not `<`).
    - `dino.js` contains `context.drawImage(track1.img, track1.x, track1.y, track1.width, track1.height);` AND the analogous line for `track2`.
    - `dino.js` does NOT contain `trackVelocityX` (D-02 — reuse `velocityX`).
    - `dino.css` contains `background-color: white;`.
    - `dino.css` does NOT contain `background-color: lightgray` (lightgray fully removed).
    - `grep -c 'requestAnimationFrame' dino.js` equals 2 (one in `window.onload`, one as the first line of `update()` — existing pattern preserved).
    - The track block in `update()` is positioned AFTER `context.clearRect(0, 0, board.width, board.height);` AND BEFORE the `//dino` comment line.
    - No physics constant changed: `dino.js` still contains `let velocityX = -8;` AND `let velocityY = 0;` AND `let gravity = .4;` AND `velocityY = -10;` (jump) AND `let dinoY = boardHeight - dinoHeight;`.
  </acceptance_criteria>
  <done>
    - All grep-verifiable acceptance criteria pass.
    - **Manual play-test (browser) — anchored to ROADMAP success criterion 1 and 3:**
      1. Open `index.html` in a browser (or `python3 -m http.server` and visit localhost).
      2. Ground strip is visible immediately at the bottom of the canvas (no gray band, no missing track).
      3. Ground scrolls smoothly left at the same apparent speed as cacti.
      4. After ~5 seconds, no visible gap, jitter, or stutter appears at the wrap seam.
      5. Upper canvas area (sky) is white, not gray.
      6. Jump on Space/ArrowUp — physics, airtime, and apex feel identical to before.
      7. Collide with a cactus — game-over fires, dead-sprite swaps in, and the track halts (does not continue scrolling).
      8. Score still increments at the same apparent rate as before.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase is a pure rendering + CSS change. No new input parsing, no network surface, no storage, no DOM injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I (Information Disclosure) | `trackImg` load via `./img/track.png` | accept | Static asset on same origin; no PII; same trust level as existing cactus images. No new attack surface. |
| T-01-01-02 | T (Tampering) | `track1`/`track2` mutable globals | accept | Mutable globals already pervasive in `dino.js` (`gameOver`, `score`, `cactusArray`); track entities follow the established pattern. Browser-only, single-page, no remote write path. |

Realistic content per planning context: this is a browser-only static game with zero network surface, zero storage, and zero user-input parsing beyond keyboard `event.code` matching. The two threats above are the full extent of net-new surface introduced by this plan, both correctly dispositioned as "accept."
</threat_model>

<verification>
**No automated test suite exists.** Verification combines grep-verifiable acceptance criteria (see each task's `<acceptance_criteria>`) with manual browser play-test.

**Manual play-test checklist (anchored to ROADMAP success criteria 1 and 3 — criterion 2 is covered by plan 01-02):**

Success criterion 1 — *"While the game is running, the ground visibly scrolls left as a tiled sequence of `track.png` (no gap, no jitter)."*
- [ ] Open `index.html`. Ground strip is visible immediately along the bottom (no gray band, no missing texture).
- [ ] Ground scrolls smoothly left at the same apparent speed as cacti.
- [ ] After ~5 seconds of play, no visible gap or 1-pixel flicker appears at the wrap seam (Pitfall 3 caught).
- [ ] Upper canvas area (sky) is white (not gray) — `dino.css` change applied.
- [ ] Ground does NOT scroll after game-over (track halts with dino/cacti — D-08).

Success criterion 3 — *"The game's overall feel — physics, collision, score increment — remains unchanged from before this phase."*
- [ ] Jump (Space/ArrowUp) height, airtime, gravity feel identical to the pre-phase build.
- [ ] Cactus collision still triggers `gameOver = true` and the dead-sprite swap.
- [ ] Score increments at the same apparent rate as before.
- [ ] No visual regression: dino and cacti render at correct positions relative to the new track strip.
- [ ] Browser console shows no errors during normal play.

**Reference:** `.planning/phases/01-scrolling-track-restart/01-RESEARCH.md` "Verification Approach (Manual Play-Test)" lists the same checklist with finer detail.
</verification>

<success_criteria>
This plan is complete when:
- [ ] All Task 1 and Task 2 acceptance criteria pass.
- [ ] `dino.js` contains the new `//track` globals block and the `//track` block in `update()`.
- [ ] `dino.css` `#board` background-color is `white`.
- [ ] Manual play-test confirms ROADMAP success criterion 1 (ground scrolls, no gap, no jitter, halts on game-over) and ROADMAP success criterion 3 (physics/collision/score unchanged).
- [ ] No `const` introduced anywhere; no module/class refactor; no physics constants modified.
- [ ] Total file size remains well under 200 lines (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrolling-track-restart/01-01-SUMMARY.md` documenting:
- Files modified with exact line ranges added.
- Manual play-test outcomes against ROADMAP success criteria 1 and 3.
- Any deviations from D-01 through D-18 (expected: none).
- Any unexpected behavior observed in the browser.
</output>
