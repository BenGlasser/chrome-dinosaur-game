---
phase: 01-scrolling-track-restart
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - dino.js
autonomous: true
requirements:
  - RESTART-01

must_haves:
  truths:
    - "While the game is running, the ground visibly scrolls left as a tiled sequence of track.png (no gap, no jitter)."
    - "After the dino dies, pressing any key clears the cacti, resets the score and dino state, and starts a new run without reloading the page."
    - "The game's overall feel — physics, collision, score increment — remains unchanged from before this phase."
  artifacts:
    - path: "dino.js"
      provides: "resetGame() helper performing the canonical D-12 state reset"
      contains: "function resetGame()"
    - path: "dino.js"
      provides: "moveDino() gameOver branch invokes resetGame() instead of plain early-return"
      contains: "resetGame();"
  key_links:
    - from: "moveDino() in dino.js"
      to: "resetGame()"
      via: "if (gameOver) { resetGame(); return; }"
      pattern: "if \\(gameOver\\) \\{\\s*resetGame\\(\\);"
    - from: "resetGame()"
      to: "module-level globals (gameOver, score, cactusArray, velocityY, dino.y, dinoImg.src)"
      via: "direct reassignment per D-12 canonical list"
      pattern: "gameOver = false"
    - from: "resetGame()"
      to: "dinoImg sprite swap-back to ./img/dino.png"
      via: "dinoImg.src = \"./img/dino.png\""
      pattern: "dinoImg\\.src = \"\\./img/dino\\.png\""
---

<objective>
Add a `resetGame()` helper to `dino.js` that performs the D-12 canonical state reset (`gameOver=false; score=0; cactusArray=[]; velocityY=0; dino.y=dinoY; dinoImg.src="./img/dino.png"`), and replace the existing `gameOver` early-return at the top of `moveDino(e)` with a branch that calls `resetGame()` and returns. This closes RESTART-01 — after game-over, ANY keypress restarts the run without reloading the page (D-09, D-10).

Purpose: Closes requirement RESTART-01 and delivers ROADMAP success criterion 2. Restart is implemented as a pure state flip — `requestAnimationFrame(update)` and `setInterval(placeCactus, 1000)` already self-perpetuate (ARCHITECTURE.md "Notable Architectural Properties"; CONCERNS.md latent bug #4), so flipping `gameOver = false` is sufficient to resume both loops (D-13).

Output: `dino.js` gains a new `resetGame()` function and a one-line behavior change inside `moveDino(e)`. No other files are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scrolling-track-restart/01-CONTEXT.md
@.planning/phases/01-scrolling-track-restart/01-RESEARCH.md
@.planning/phases/01-scrolling-track-restart/01-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@dino.js

<interfaces>
<!-- Existing globals this plan READS and WRITES. Do NOT redeclare these. -->

Globals reset by `resetGame()` (per D-12 canonical list):
- `gameOver` (dino.js:42, currently `false` at module load) — write `false`
- `score` (dino.js:43, currently `0`) — write `0`
- `cactusArray` (dino.js:23, currently `[]`) — reassign to `[]`
- `velocityY` (dino.js:39, currently `0`) — write `0`
- `dino` object (dino.js:15-20) — write `dino.y = dinoY`
- `dinoImg` (dino.js:13, the shared sprite reference) — write `dinoImg.src = "./img/dino.png"`

Globals NOT reset (kept stable per RESEARCH.md "State That Survives Restart"):
- `velocityX`, `gravity`, `dinoX`, `dinoWidth`, `dinoHeight`, `dinoY` (the ground constant)
- `track1.x`, `track2.x` (introduced by plan 01-01 — track scrolls continuously and remains valid)
- `boardWidth`, `boardHeight`, `context`, `board`

Existing call site this plan MODIFIES:
- `moveDino(e)` (dino.js:110-123) — replace the `if (gameOver) { return; }` early-return at lines 111-113 with the restart branch.

New function this plan CREATES:
- `resetGame()` — placed after `placeCactus` (currently dino.js:125-160) and before `detectCollision` (currently dino.js:162-167). Per D-17, a named helper rather than inlining keeps the reset list discoverable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add resetGame() helper performing the D-12 canonical state reset</name>
  <files>dino.js</files>
  <read_first>
    - dino.js (full file, ~167 lines pre-Task — read once before editing)
    - .planning/phases/01-scrolling-track-restart/01-CONTEXT.md sections D-12, D-13, D-15, D-17
    - .planning/phases/01-scrolling-track-restart/01-RESEARCH.md "Touchpoint 4" (the literal `resetGame()` body), "State That Survives Restart"
    - .planning/codebase/CONCERNS.md "Fragile Areas #3" (the dinoImg.src swap pattern — restart MUST swap back to dino.png)
  </read_first>
  <behavior>
    - After this task: `dino.js` defines a top-level function `resetGame()` that, when called, performs the canonical D-12 state reset (and ONLY that reset).
    - `resetGame()` is NOT yet wired (Task 2 wires it). Calling it manually from a browser console would correctly reset state; no behavior change is observable in normal play.
    - The function lives between `placeCactus` and `detectCollision` per D-17 logical grouping.
    - Track positions (`track1.x`, `track2.x`) are intentionally NOT reset — they continuously scroll and remain valid.
  </behavior>
  <action>
**Edit 1 — Add `resetGame()` function between `placeCactus` and `detectCollision`.**

Locate the end of `placeCactus()` in `dino.js` (the closing `}` of `placeCactus`, currently on line 160). Locate the start of `detectCollision(a, b)` (currently on line 162).

Insert a new function between them. The exact code to insert (per D-12 verbatim and RESEARCH.md "Touchpoint 4"):

```javascript

function resetGame() {
    gameOver = false;
    score = 0;
    cactusArray = [];
    velocityY = 0;
    dino.y = dinoY;
    dinoImg.src = "./img/dino.png";
}
```

Style/correctness notes:
- Indentation: 4 spaces, matching `placeCactus` and `detectCollision` bodies.
- Order of statements in the function body: match the D-12 canonical list verbatim (`gameOver`, `score`, `cactusArray`, `velocityY`, `dino.y`, `dinoImg.src`). This is the documented reset order; preserve it for readability and future-grep.
- `dino.y = dinoY` — uses the ground constant `dinoY` (dino.js:12). DO NOT use any other value (e.g., do not hard-code `156`). The constant is the single source of truth (CONCERNS.md fragile area #5 — `dinoY` is overloaded as both initial-y and ground-clamp).
- `dinoImg.src = "./img/dino.png"` — undoes the dead-sprite swap from `dino.js:96`. Use the exact same path the initial load uses at `dino.js:57`.
- `cactusArray = []` — reassign to a new empty array. The CONTEXT.md notes either `= []` or `.length = 0` is acceptable (D-12, RESEARCH.md Pitfall 4); use `= []` for readability and to match the initial declaration `let cactusArray = []` at dino.js:23.
- Use `let`-style globals; do NOT introduce `const` anywhere. Do NOT add JSDoc comments — the codebase doesn't use them.

**DO NOT:**
- Do NOT reset `track1.x` / `track2.x`. Track continues scrolling — D-12 explicitly excludes these.
- Do NOT reset `velocityX`, `gravity`, `dinoX`, `dinoWidth`, `dinoHeight`, or any physics/dimension constant.
- Do NOT call `clearInterval(...)` or `clearTimeout(...)`. Do NOT call `requestAnimationFrame(...)`. Both loops self-perpetuate (D-13). Restart is a pure state flip.
- Do NOT call `placeCactus()` directly to "seed" a new run — let `setInterval` deliver the next cactus on its natural cadence (D-13, RESEARCH.md Risk 4 — "dead time on restart" is acceptable).
- Do NOT move existing functions (`placeCactus`, `detectCollision`, `moveDino`, `update`).
- Do NOT modify `moveDino(e)` in this task — Task 2 owns the wiring.
  </action>
  <verify>
    <automated>cd /Users/benglasser/git/chrome-dinosaur-game &amp;&amp; node --check dino.js &amp;&amp; grep -q '^function resetGame() {' dino.js &amp;&amp; grep -q 'gameOver = false;' dino.js &amp;&amp; grep -q 'score = 0;' dino.js &amp;&amp; grep -q 'cactusArray = \[\];' dino.js &amp;&amp; grep -q 'velocityY = 0;' dino.js &amp;&amp; grep -q 'dino.y = dinoY;' dino.js &amp;&amp; grep -q 'dinoImg.src = "./img/dino.png";' dino.js &amp;&amp; ! grep -q 'clearInterval' dino.js &amp;&amp; ! grep -q 'const resetGame' dino.js &amp;&amp; awk '/^function resetGame/{r=NR} /^function detectCollision/{d=NR} /^function placeCactus/{p=NR} END{exit !(p &lt; r &amp;&amp; r &lt; d)}' dino.js &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check dino.js` exits 0 (file parses).
    - `dino.js` contains a line matching `^function resetGame() {`.
    - The body of `resetGame()` contains, in this order: `gameOver = false;`, `score = 0;`, `cactusArray = [];`, `velocityY = 0;`, `dino.y = dinoY;`, `dinoImg.src = "./img/dino.png";`.
    - `dino.js` contains the literal string `dinoImg.src = "./img/dino.png"` (the restart sprite swap-back — closes Risk 2 in RESEARCH.md).
    - `dino.js` does NOT contain `clearInterval` or any new `requestAnimationFrame` call (D-13 — loops self-perpetuate).
    - `dino.js` does NOT contain `const resetGame` (preserves tutorial style — D-16; CLAUDE.md).
    - `resetGame` is defined AFTER `placeCactus` AND BEFORE `detectCollision` in source order.
    - `resetGame()` does NOT reset `track1`, `track2`, `velocityX`, `gravity`, `dinoX`, `dinoWidth`, or `dinoHeight` (RESEARCH.md "State That Survives Restart").
  </acceptance_criteria>
  <done>
    - All grep-verifiable acceptance criteria pass.
    - File parses with `node --check dino.js`.
    - Browser load of `index.html` still runs the game with no console errors.
    - `resetGame` is callable from the browser console (manual sanity check, optional): after a game-over, `resetGame()` typed in DevTools console restarts the run.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire any-keydown restart by replacing the gameOver early-return in moveDino()</name>
  <files>dino.js</files>
  <read_first>
    - dino.js (re-read after Task 1 — focus on `moveDino()` at dino.js:110-123 and confirm `resetGame()` is defined)
    - .planning/phases/01-scrolling-track-restart/01-CONTEXT.md sections D-09, D-10, D-11
    - .planning/phases/01-scrolling-track-restart/01-RESEARCH.md "Touchpoint 4" (the moveDino edit), "Risk 3" (key-repeat is acceptable per D-11)
  </read_first>
  <behavior>
    - After this task: when `gameOver === true`, the next `keydown` event (any key — D-09) calls `resetGame()` and returns. The dino's running state, score, cactus list, velocity, position, and sprite are reset; the game resumes on the next animation frame and the next `placeCactus` tick (both already running per D-13).
    - When `gameOver === false`, `moveDino(e)` behaves exactly as before — Space/ArrowUp jump branch is unchanged; ArrowDown branch (currently empty placeholder) is unchanged. No new key handling is introduced.
    - Holding Space at the moment of collision causes the next key-repeat to trigger restart immediately. Per D-11, this is acceptable behavior; do not add debounce.
  </behavior>
  <action>
**Edit 1 — Replace the gameOver early-return in `moveDino(e)`.**

Locate `moveDino(e)` in `dino.js` (currently lines 110-123). The current top of the function is:

```javascript
function moveDino(e) {
    if (gameOver) {
        return;
    }

    if ((e.code == "Space" || e.code == "ArrowUp") && dino.y == dinoY) {
        //jump
        velocityY = -10;
    }
    else if (e.code == "ArrowDown" && dino.y == dinoY) {
        //duck
    }

}
```

Modify ONLY the gameOver block (the first `if`). Replace:

```javascript
    if (gameOver) {
        return;
    }
```

with:

```javascript
    if (gameOver) {
        resetGame();
        return;
    }
```

DO NOT modify any other line in `moveDino`. The Space/ArrowUp jump branch stays exactly as-is. The ArrowDown placeholder branch stays exactly as-is (Phase 2 owns it). The trailing blank line and closing `}` stay as-is.

Notes:
- D-10 explicitly locks "reuse existing `moveDino(e)` listener — do NOT add a second listener". DO NOT call `document.addEventListener("keydown", ...)` anywhere in this task.
- D-09: any key triggers restart. The branch is gated on `gameOver`, NOT on `e.code`. Letter keys, arrow keys, modifiers — all trigger reset.
- D-11: no debounce. Do not add a `lastResetAt` timestamp or any cooldown logic. Key-repeat firing immediately after death is acceptable.
- The `return;` after `resetGame();` is required — without it, control would fall through to the jump branch, which would check `dino.y == dinoY`. After `resetGame()` runs, `dino.y` IS `dinoY`, so a held Space would actually fire a jump on the very first restart frame. That's an unintended side-effect. Keep the `return;` to prevent fall-through.

**DO NOT:**
- Do NOT add a separate listener.
- Do NOT add input debounce, cooldown, or `e.code` filtering.
- Do NOT modify the jump (Space/ArrowUp) branch.
- Do NOT modify the ArrowDown placeholder branch (that's Phase 2 — DINO-03/DINO-04).
- Do NOT introduce `const`.
- Do NOT change physics constants.
  </action>
  <verify>
    <automated>cd /Users/benglasser/git/chrome-dinosaur-game &amp;&amp; node --check dino.js &amp;&amp; grep -Pzo '(?s)if \(gameOver\) \{\s*resetGame\(\);\s*return;\s*\}' dino.js &gt;/dev/null &amp;&amp; grep -q 'velocityY = -10;' dino.js &amp;&amp; [ "$(grep -c 'document.addEventListener' dino.js)" = "1" ] &amp;&amp; [ "$(grep -c 'function moveDino' dino.js)" = "1" ] &amp;&amp; [ "$(grep -c 'function resetGame' dino.js)" = "1" ] &amp;&amp; ! grep -q 'e.code ==' dino.js | grep -v -E '(Space|ArrowUp|ArrowDown)' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check dino.js` exits 0 (file parses).
    - `dino.js` contains the literal sequence (whitespace-tolerant) `if (gameOver) { resetGame(); return; }` inside `moveDino`.
    - `dino.js` still contains `velocityY = -10;` (jump velocity unchanged — Success Criterion #3).
    - `dino.js` still contains `e.code == "Space" || e.code == "ArrowUp"` (jump branch untouched).
    - `dino.js` still contains `e.code == "ArrowDown"` (duck placeholder untouched — Phase 2 owns it).
    - `grep -c 'document.addEventListener' dino.js` equals 1 (still ONE keydown listener — D-10 honored, no second listener added).
    - `grep -c 'function moveDino' dino.js` equals 1.
    - `grep -c 'function resetGame' dino.js` equals 1.
    - `dino.js` does NOT contain any restart cooldown / debounce logic (no `lastResetAt`, no `Date.now()`, no `setTimeout` introduced for input gating).
    - No physics constant changed: `let velocityX = -8;`, `let velocityY = 0;`, `let gravity = .4;`, `let dinoY = boardHeight - dinoHeight;`, and `velocityY = -10;` (the jump assignment) are all present.
  </acceptance_criteria>
  <done>
    - All grep-verifiable acceptance criteria pass.
    - **Manual play-test (browser) — anchored to ROADMAP success criterion 2 and 3:**
      1. Open `index.html` in a browser.
      2. Play until the dino collides with a cactus (game-over fires, dead-sprite shows, scroll halts).
      3. Press `Space` — game resumes immediately: dino is back at ground level, alive sprite, score is `0`, all cacti gone.
      4. Play, die again. Press `ArrowUp` — same restart behavior.
      5. Play, die again. Press a letter key (e.g., `a`) — same restart behavior (D-09: any key).
      6. After restart, new cacti spawn within ~0–1000 ms (depending on `setInterval` phase — D-13/Risk 4 is acceptable).
      7. After restart, the track continues scrolling normally (track positions kept across restart per D-12).
      8. Die-and-restart loop is stable across at least 3 cycles — no console errors, no freezes.
      9. Jump physics (Space/ArrowUp) and apex height feel identical to before this phase (Success Criterion #3).
      10. Score still increments at the same apparent rate as before; collision still fires; dead-sprite still appears on collision (Success Criterion #3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Browser-only static game. Restart is purely a state flip on module-level globals in response to a `keydown` event already handled by the existing listener. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T (Tampering) | `resetGame()` global state writes | accept | All writes target existing module-level globals already mutated by `update()`, `placeCactus()`, and `moveDino()`. Same trust level as existing code. No DOM injection, no eval, no remote write. |
| T-01-02-02 | D (Denial of Service) | Key-repeat firing `resetGame()` rapidly when Space is held at game-over | accept | Per D-11, this is intended behavior. `resetGame()` is O(1) — six writes plus an array reallocation — and runs synchronously inside the keydown handler. No measurable resource consumption even at maximum browser key-repeat rates (~30 ms intervals). |

Realistic content per planning context: zero network surface, zero storage, zero injection paths, keyboard `event.code` is read-only branch logic. The two threats above are the full extent of net-new surface introduced by this plan, both correctly dispositioned as "accept."
</threat_model>

<verification>
**No automated test suite exists.** Verification combines grep-verifiable acceptance criteria (see each task's `<acceptance_criteria>`) with manual browser play-test.

**Manual play-test checklist (anchored to ROADMAP success criteria 2 and 3 — criterion 1 is covered by plan 01-01):**

Success criterion 2 — *"After the dino dies, pressing any key clears the cacti, resets the score and dino state, and starts a new run without reloading the page."*
- [ ] Collide with a cactus. Dead-sprite appears. Cacti and (after plan 01-01) the track halt.
- [ ] Press `Space`. Game immediately resumes: dino is back at ground level, alive sprite (`dino.png`, not `dino-dead.png`), score is `0`, all cacti gone.
- [ ] Press `ArrowUp` after a death. Same result.
- [ ] Press an arbitrary letter key (e.g., `a`) after a death. Same result (D-09 — any key).
- [ ] After restart, new cacti spawn normally within ~0–1000 ms (timer phase preserved per D-13/Risk 4).
- [ ] After restart, the dino can jump and die and restart again — loop is stable across at least 3 die-and-restart cycles.
- [ ] Browser console shows no errors during die-and-restart.

Success criterion 3 — *"The game's overall feel — physics, collision, score increment — remains unchanged from before this phase."*
- [ ] Jump (Space/ArrowUp) height, airtime, gravity feel identical to the pre-phase build.
- [ ] Cactus collision still triggers `gameOver = true` and the dead-sprite swap.
- [ ] Score still increments at the same apparent rate (the frame-rate-dependent score bug is intentionally preserved per D-14 — out of scope).
- [ ] No visual regression: dino, cacti, score text render at correct positions.
- [ ] After restart, the track (from plan 01-01) is still scrolling correctly — track positions intentionally NOT reset (D-12, RESEARCH.md "State That Survives Restart").

**Reference:** `.planning/phases/01-scrolling-track-restart/01-RESEARCH.md` "Verification Approach (Manual Play-Test)" lists the same checklist with finer detail.
</verification>

<success_criteria>
This plan is complete when:
- [ ] All Task 1 and Task 2 acceptance criteria pass.
- [ ] `dino.js` defines `function resetGame()` performing the D-12 canonical reset list verbatim.
- [ ] `moveDino(e)`'s gameOver branch calls `resetGame(); return;` instead of plain `return;`.
- [ ] Manual play-test confirms ROADMAP success criterion 2 (any-key restart works for Space, ArrowUp, and a letter key; loop stable for at least 3 die-restart cycles) and continues to satisfy ROADMAP success criterion 3 (physics/collision/score unchanged).
- [ ] No `const` introduced; no module/class refactor; no physics constants modified; no second keydown listener; no input debounce.
- [ ] Total file size remains well under 200 lines (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrolling-track-restart/01-02-SUMMARY.md` documenting:
- Files modified with exact line ranges added/changed.
- Manual play-test outcomes against ROADMAP success criteria 2 and 3 (criterion 1 was covered by 01-01).
- Confirmation that the integrated post-01-01-and-01-02 build satisfies all three ROADMAP success criteria (track scrolls, restart works, feel unchanged).
- Any deviations from D-09 through D-17 (expected: none).
- Any unexpected behavior observed in the browser, including any encounters with the documented Risk 5 (`dinoImg.src` swap race) — D-15 documents this as acceptable; report only if it visibly manifests.
</output>
